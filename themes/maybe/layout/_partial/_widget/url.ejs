<% if (is_category('read')){ %>
    <% if (post.content.length != 0){ %>
        <%- url_for(post.path) %>
    <% }else { %>
        ""
    <% } %>
<% }else{ %>
    <% if (post.list != null && post.list != ""){ %>
        /tags/<%- post.list %>
    <% }else { %>
        <%- url_for(post.path) %>
    <% } %>
<% } %>
